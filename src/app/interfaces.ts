export interface GrammarPoint {
    chapter: number,
    point: number,
    grammar: string
};

export interface VocabularyPoint {
    chapter: number,
    kanji: string,
    reading: string,
    meaning: string,
}